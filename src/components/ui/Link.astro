---
import { buttonVariants } from "../../styles/tv/buttonVariants";

const { text, variant, href, target, rel } = Astro.props;
---

<a href={href} target={target} rel={rel} class={buttonVariants({ buttonVariant: variant })}
  >{text}<slot /></a
>
